#include<iostream>
using namespace std;
int main()
{
    int n,sum=0,a;
    cin>>n;
    int temp=n;
    while(n>0)
    {
        a=n%10;    
        sum=(sum*10)+a;    
        n=n/10; 
    }
    if(temp==sum)
    {
        cout<<"Same";
    }
    else
    {
        cout<<"Not Same";
    }
}
