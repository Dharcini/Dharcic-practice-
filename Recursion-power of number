#include<iostream>
#include<math.h>
using namespace std;
int main()
{
    int n,p,ans;
    cin>>n>>p;
    for(int i=0;i<=p;i++)
    {
        ans=pow(n,i);
    }
    cout<<n<<" to the power "<<p<<" is "<<ans;
}

